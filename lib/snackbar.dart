import 'package:flutter/material.dart';

const SnackBar snackbarOne = SnackBar(content: Text('Alert has been Pressed!',
style: TextStyle(fontSize: 30)));

const SnackBar snackbarTwo = SnackBar(content: Text('Search has been Pressed!',
style: TextStyle(fontSize: 30)));

const SnackBar snackbarThree = SnackBar(content: Text('Navigation has been Pressed!',
style: TextStyle(fontSize: 30)));

const SnackBar snackbarFour = SnackBar(content: Text('You have chosen one. You are at safe zone!',
style: TextStyle(fontSize: 30),));

const SnackBar snackbarFive = SnackBar(content: Text('You have chosen two. You need to be careful!',
style: TextStyle(fontSize: 30),),);

const SnackBar snackbarSix = SnackBar(content: Text('You have chosen three. You are at danger zone!',
style: TextStyle(fontSize: 30),),); 


